---
label: Bio Post
hide_body: true
fields:
- name: bio
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: bio
- name: image
  type: file
  config:
    maxSize: 64
  label: image
